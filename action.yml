name: 'Generic helm pack and Push to ACR y SWR'
description: 'Helm pack and push to acr y swr'
inputs:
  ACR_USERNAME:
    description: "user de acr"
    required: true
  ACR_PASSWORD:
    description: "passwrd de acr"
    required: true
  ACR_FULL_NAME:
    description: "nombre del acr con azurecr.io"
    required: true
  appName:
    description: "Nombre de la aplicacion (dentro del proyecto)"
    required: true
  namespace:
    description: "nombre de namespace"
    required: false
    default: ""
  teamProject:
    description: "Nombre del proyecto/equipo"
    required: true
  appVersion:
    description: "tag para subir imagen"
    required: true
  helmChartVersionLatest:
     description: "VersionHelm"
     required: true
 
 #SWR
  SWR_USERNAME:
    description: "user de acr"
    required: true
  SWR_PASSWORD:
    description: "password de acr"
    required: true
  SWR_NAME:
    description: "nombre de la organizacion de SWR"
    required: true
  SWR_URL:
    description: "url de swr"     
    required: true
    
runs:
  using: "composite"
  steps:
      - run: echo 'Inicia el proceso'
        shell: bash
      
      - name: Install Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.12.3 #Helm 13 tiene un bug
      
      - name: Reviso archivos y version helm
        shell: bash
        run: |-
           ls
           helm version --short

      - uses: actions/download-artifact@v4
        with:
          name: ${{inputs.appName}}-${{inputs.namespace}}

    # Verifica helm por errores (deberia ser bloqueante). Revisar este paso con un Json para asegurar que esté correcto.
      - name: Helm Lint 
        shell: bash
        run: |-
          helmLint="helm lint --values=values.yaml > /dev/null 2>&1"
          if eval "$helmLint"; then
             echo "HELM Lint OK - The template folder and values file format is OK";
          else
             echo "HELM Lint FAIL - There is a problem with the folder 'templates' or the values file format";
             exit 1
          fi

      - name: Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ inputs.ACR_NAME }}.azurecr.io #ACR_FULL_NAME incluye el azurecr.io
          username: ${{ inputs.ACR_USERNAME }}
          password: ${{ inputs.ACR_PASSWORD }}
      - name: Enpaquetar y subir helm a acr
        shell: bash
        run: |-
          export HELM_EXPERIMENTAL_OCI=1 #Es necesario para el chart save}}
          helm package --version ${{ inputs.helmChartVersionLatest}} --app-version ${{ inputs.appVersion}} --namespace ${{ inputs.namespace}} .
          echo '${{ inputs.ACR_PASSWORD }}' | helm registry login ${{ inputs.ACR_FULL_NAME}} --username ${{ inputs.ACR_USERNAME}} --password-stdin
          helm push ${{inputs.TeamProject}}-${{inputs.appName }}-${{inputs.helmChartVersionLatest}}.tgz  oci://${{inputs.ACR_FULL_NAME }}/helm/ # ${{ inputs.TeamProject}}-${{ inputs.appName}}  # :${{ env.helmChartVersionLatest}}
     
      - name: Empaquetar y subir Helm chart a Huawei SWR
        shell: bash
        run: |
          helm package --version ${{ inputs.helmChartVersionLatest }} --app-version ${{ inputs.appVersion }} --namespace ${{ inputs.namespace }} .
          CHART_FILE="${{ inputs.teamProject }}-${{ inputs.appName }}-${{ inputs.helmChartVersionLatest }}.tgz"

          echo "Subiendo a: https://${{ inputs.SWR_URL }}/chartrepo/helm/charts"

          STATUS=$(curl -s -o /tmp/helm_resp.json -w "%{http_code}" -X POST "https://${{ inputs.SWR_URL }}/chartrepo/helm/charts" -u "${{ inputs.SWR_USERNAME }}:${{ inputs.SWR_PASSWORD }}" -F "chart=@${CHART_FILE}")

          echo "Status: $STATUS"
          cat /tmp/helm_resp.json

          if [[ "$STATUS" != "201" && "$STATUS" != "200" ]]; then
            echo "Error al subir el Helm chart"
            exit 1
          fi 1
          fi

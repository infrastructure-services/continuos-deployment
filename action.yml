name: 'Container Vulnerability Scan'
description: 'Step to Scan Vulnerability in docker image generate in this process'
inputs:
  appName:
    description: "nombre de aplicación"
    required: true
  teamProject:
    description: "nombre de proyecto"
    required: true
  appVersion:
    description: "Versión de la aplicación"
    required: true
  registry:
    descripción: "Container Registry Name"
    required: true

runs:
  using: "composite"
  steps:
      - run: |
          echo "appName: ${{ inputs.appName }}"
          echo "teamProject: ${{ inputs.teamProject }}"
          echo "appVersion: ${{ inputs.appVersion }}"
          echo "registry: ${{ inputs.registry }}"
        shell: bash
      - run: echo "✅ Success" 
        shell: bash
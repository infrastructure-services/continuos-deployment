name: 'Generate values.yaml'
description: 'Action to generate values.yaml file from .devops values-env.yaml file and repository variables'
inputs:

  APPS_CERTIFICATE:
    description: "Certificado para el dominio de apps en ARO"
    required: true
  APPS_CA_CERTIFICATE:
    description: "Certificado de la CA"
    required: true
  APPS_KEY_CERTIFICATE:
    description: "Certificado ARO Key"
    required: true
    
  GITHUB_HELMREPO_TOKEN:
    description: "Token para descargar el codigo de templates HELM"
    required: true
  helmRepo:
    description: "Repositorio de helm"
    required: false
    default: "infrastructure-services/manifiestos-cd"
  helmRepoRef:
    description: "Ref del repositorio de Helm"
    required: false
    default: "standard"
    
  appName:
    description: "Nombre de la aplicacion (dentro del proyecto)"
    required: true
  namespace:
    description: "nombre de namespace"
    required: false
    default: ""
    
  clusterFqdn:
     description: "FQDN para pasarle a helm"
     required: true
  fileValuesPath:
    description: "path archivo de values.yaml"
    required: true
  teamProject:
    description: "Nombre del proyecto/equipo"
    required: true
    
  cpuLimit:
    description: "Limite de CPU del pod"
    required: false
    default: "1"
  memLimit:
    description: "Limite de Memoria del Pod"
    required: false
    default: "1Gi"
  cpuRequest:
    description: "Request de CPU del pod"
    required: false
    default: "40m"
  memRequest:
    description: "Request de Memoria del Pod"
    required: false
    default: "300Mi"

  # AZURE KEY VAULT
  AZURE_KEYVAULT_NAME:
    description: "Nombre del keyvault"
    required: false
    default: ""
  AZURE_KEYVAULT_TENANT:
    description: "Tenant del keyvault"
    required: false
    default: ""
  AZURE_KEYVAULT_CLIENT_ID:
    description: "Client ID del keyvault"
    required: false
    default: ""
  AZURE_KEYVAULT_SECRET:
    description: "Secret del keyvault"
    required: false
    default: ""
  AZURE_KEYVAULT_PREFIX:
    description: "Prefijo de las variables de la app en keyvault"
    required: false
    default: ""

  #Observability Variables
  ELASTIC_APM_ACTIVE:
    description: "APM Activo"
    required: false
    default: "true"
  ELASTIC_APM_ENABLED:
    description: "APM Enabled"
    required: false
    default: "true"
  REACT_APP_DISABLE_APM:
    description: "APM Desactivado para las apps de React"
    required: false
    default: "0"
  NEXT_PUBLIC_DISABLE_APM:
    description: "APM Desactivado para las apps de nextJs"
    required: false
    default: "0"
  APM_SERVER_URL:
    description: "Url de APM"
    required: false
    default: "https://apm-mon.andreani.com"
  APM_SERVICE_NAME:
    description: "Service Name específico (Opcional, no se usa)"
    required: false
    default: ""

runs:
  using: "composite"
  steps:
    - run: echo '🎁Generate values.yaml'
      shell: bash

    - name: Install Helm
      uses: azure/setup-helm@v3
      with:
        version: v3.12.3 #Helm 13 tiene un bug (5/11/2024 Hay que chequear si todavía está)
        
    - name: checkout codigo
      uses: actions/checkout@v6

    - name: Copy values.yaml
      shell: bash
      run: |-
          ls ../
          cp ${{ inputs.fileValuesPath }} ../values.yaml

    - name: Checkout code helm templates
      uses: actions/checkout@v6
      with:
        repository: ${{ inputs.helmRepo }}
        ref: ${{ inputs.helmRepoRef }}
        token: ${{ inputs.GITHUB_HELMREPO_TOKEN }}
    
    - name: Restore values.yaml
      shell: bash
      run: cp ../values.yaml values.yaml
    
    - name: Helm customizer le agregan valores al values
      shell: bash
      run: |-
        echo -e 'cluster:\n  fqdn: ${{ inputs.clusterFqdn}}' >> values.yaml
        ls
        echo 'namespace: ${{inputs.namespace}}' >> values.yaml
        echo 'projectName: ${{inputs.teamProject}}' >> values.yaml
        echo 'appname: ${{inputs.appName}}' >> values.yaml
        echo 'cpulimit: ${{inputs.cpuLimit}}' >> values.yaml
        echo 'memlimit: ${{inputs.memLimit}}' >> values.yaml
        echo 'cpurequest: ${{inputs.cpuRequest}}' >> values.yaml
        echo 'memrequest: ${{inputs.memRequest}}' >> values.yaml
        echo 'caCertificate: |' >> values.yaml
        echo -e "${{inputs.APPS_CA_CERTIFICATE}}" | sed 's/^/    /' >> values.yaml
        echo 'certificate: |' >> values.yaml
        echo -e "${{inputs.APPS_CERTIFICATE}}" | sed 's/^/    /' >> values.yaml
        echo 'key: |' >> values.yaml
        echo -e "${{inputs.APPS_KEY_CERTIFICATE}}" |  sed 's/^/    /' >> values.yaml
        echo 'name: ${{inputs.teamProject}}-${{inputs.appName}}' >> Chart.yaml

        # 🚀 Agregás tu sección "env_default"
        echo 'env_default:' >> values.yaml
        echo '  ELASTIC_APM_ACTIVE: ${{ inputs.ELASTIC_APM_ACTIVE }}' >> values.yaml
        echo '  ELASTIC_APM_ENABLED: ${{ inputs.ELASTIC_APM_ENABLED }}' >> values.yaml
        echo '  REACT_APP_DISABLE_APM: ${{ inputs.REACT_APP_DISABLE_APM }}' >> values.yaml
        echo '  NEXT_PUBLIC_DISABLE_APM: ${{ inputs.NEXT_PUBLIC_DISABLE_APM }}' >> values.yaml
        echo '  ELASTIC_APM_SERVER_URL: "${{inputs.APM_SERVER_URL}}"' >> values.yaml
        echo '  ELASTIC_APM_SERVICE_NAME: ${{inputs.namespace}}_${{inputs.appName}}' >> values.yaml
        echo '  Observability__ServerUrls: "${{inputs.APM_SERVER_URL}}"' >> values.yaml
        echo '  Observability__ServerUrl: "${{inputs.APM_SERVER_URL}}"' >> values.yaml
        echo '  Observability__ServiceName: ${{inputs.namespace}}_${{inputs.appName}}' >> values.yaml
        echo '  REACT_APP_ELASTIC_APM_ACTIVE: ${{ inputs.ELASTIC_APM_ACTIVE }}' >> values.yaml
        echo '  REACT_APP_ELASTIC_APM_ENABLED: ${{ inputs.ELASTIC_APM_ENABLED }}' >> values.yaml
        echo '  REACT_APP_ELASTIC_APM_SERVER_URL: "${{inputs.APM_SERVER_URL}}"' >> values.yaml
        echo '  REACT_APP_ELASTIC_APM_SERVICE_NAME: ${{inputs.namespace}}_${{inputs.appName}}' >> values.yaml

    - name: Inject Azure Key Vault if exists
      shell: bash
      env: 
        AZURE_KEYVAULT_CLIENT_ID: ${{ inputs.AZURE_KEYVAULT_CLIENT_ID }}
        AZURE_KEYVAULT_SECRET: ${{ inputs.AZURE_KEYVAULT_SECRET }}
        AZURE_KEYVAULT_NAME: ${{ inputs.AZURE_KEYVAULT_NAME }}
        AZURE_KEYVAULT_TENANT: ${{ inputs.AZURE_KEYVAULT_TENANT }}
        AZURE_KEYVAULT_PREFIX: ${{ inputs.AZURE_KEYVAULT_PREFIX }}
      if: ${{ inputs.AZURE_KEYVAULT_NAME != '' && inputs.AZURE_KEYVAULT_TENANT != '' && inputs.AZURE_KEYVAULT_CLIENT_ID != '' && inputs.AZURE_KEYVAULT_SECRET != '' }}
      run: |
        sed -i "/replicacontroller:/i \    AzureKeyVault__Name: ${AZURE_KEYVAULT_NAME}" values.yaml
        sed -i "/replicacontroller:/i \    AzureKeyVault__TenantId: ${AZURE_KEYVAULT_TENANT}" values.yaml
        sed -i "/replicacontroller:/i \    AzureKeyVault__ClientId: ${AZURE_KEYVAULT_CLIENT_ID}" values.yaml
        sed -i "/replicacontroller:/i \    AzureKeyVault__ClientSecret: ${AZURE_KEYVAULT_SECRET}" values.yaml
        sed -i "/replicacontroller:/i \    AzureKeyVault__Prefix: ${AZURE_KEYVAULT_PREFIX}" values.yaml

    - name: show values
      shell: bash
      run: cat values.yaml

    - name: show Chart
      shell: bash
      run: cat Chart.yaml
      
    - name: Validate Resulting values.yaml
      shell: bash
      run: |
        #Validando values.yaml
        if ! command -v yq >/dev/null 2>&1; then
          echo "ERROR: falta instalar yq en el Runner - Contactar a DevOps"
          echo "Requiere yq v4: https://github.com/mikefarah/yq"
          exit 2
        fi

        validate_file() {
          local file="values.yaml"
          local has_errors=0
          local tmp_dir

          if [[ ! -f "$file" ]]; then
            echo "ERROR: no existe el archivo: $file"
            return 1
          fi

          if ! yq e '.' "$file" >/dev/null 2>&1; then
            echo ":error: YAML inválido - Revisar el formato del values"
            return 1
          fi

          tmp_dir="$(mktemp -d)"

          local normal_keys="$tmp_dir/env_normal.keys"
          local secret_keys="$tmp_dir/env_secret.keys"
          local default_keys="$tmp_dir/env_default.keys"

          : > "$normal_keys"
          : > "$secret_keys"
          : > "$default_keys"

          extract_section_keys() {
            local section="$1"
            local output_file="$2"
            local section_type

            section_type="$(yq e ".$section | type" "$file" 2>/dev/null || echo "!!null")"

            if [[ "$section_type" == "!!null" ]]; then
              return 0
            fi

            if [[ "$section_type" != "!!map" ]]; then
              echo "ERROR: $file -> la sección '$section' existe pero no es un mapa clave/valor"
              echo "::error:: Error de variables en '$section' - Revisa el values del ambiente"
              has_errors=1
              return 0
            fi

            yq e -r ".$section | keys | .[]" "$file" > "$output_file"
          }

          validate_internal_duplicates() {
            local section="$1"
            local keys_file="$2"
            local duplicated_keys

            duplicated_keys="$(sort "$keys_file" | uniq -d || true)"

            if [[ -n "$duplicated_keys" ]]; then
              echo "ERROR: $file -> variables duplicadas dentro de '$section':"
              echo "::error:: Se encontraron variables duplicadas dentro de '$section'. Mirar log del wofklow para una lista y revisar el archivo values.yaml para eliminarlas:"
              while IFS= read -r key; do
                [[ -z "$key" ]] && continue
                echo "  - $key"
              done <<< "$duplicated_keys"

              has_errors=1
            fi
          }

          validate_cross_duplicates() {
            local section_a="$1"
            local file_a="$2"
            local section_b="$3"
            local file_b="$4"
            local duplicated_keys

            duplicated_keys="$(comm -12 <(sort -u "$file_a") <(sort -u "$file_b") || true)"

            if [[ -n "$duplicated_keys" ]]; then
              echo "ERROR: $file -> variables duplicadas entre '$section_a' y '$section_b':"
              echo "::error:: Se encontraron variables duplicadas en $section_a. Mirar log del wofklow para una lista y revisar el archivo values.yaml para eliminarlas:"
              while IFS= read -r key; do
                [[ -z "$key" ]] && continue
                echo "  - $key"
              done <<< "$duplicated_keys"

              has_errors=1
            fi
          }

          extract_section_keys "env.normal" "$normal_keys"
          extract_section_keys "env.secret" "$secret_keys"
          extract_section_keys "env_default" "$default_keys"

          validate_internal_duplicates "env.normal" "$normal_keys"
          validate_internal_duplicates "env.secret" "$secret_keys"
          validate_internal_duplicates "env_default" "$default_keys"

          validate_cross_duplicates "env.normal" "$normal_keys" "env.secret" "$secret_keys"
          validate_cross_duplicates "env.secret" "$secret_keys" "env_default" "$default_keys"

          rm -rf "$tmp_dir"

          if [[ "$has_errors" -eq 0 ]]; then
            echo "OK: $file no contiene variables duplicadas no permitidas"
          fi

          return "$has_errors"
        }

        global_errors=0
        if ! validate_file "values.yaml"; then
          global_errors=1
        fi
        exit "$global_errors"
        
    - name: Archive values yaml
      uses: actions/upload-artifact@v6
      with:
        name: ${{inputs.appName}}-${{inputs.namespace}}
        overwrite: true
        path: |
          values.yaml
          Chart.yaml
          templates/






